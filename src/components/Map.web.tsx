import React from 'react';
import { View, Text } from 'react-native';

const Map = () => {
    return (
        <View className="flex-1 justify-center items-center bg-gray-200 rounded-2xl w-full h-full">
            <Text className="text-gray-500 font-JakartaMedium">Map is not supported on Web</Text>
        </View>
    );
};

export default Map;
